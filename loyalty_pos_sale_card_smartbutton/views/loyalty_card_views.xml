<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vue d'arbre pour afficher les loyalty cards liées à une commande -->
    <!-- <record id="action_loyalty_cards_from_sale_order" model="ir.actions.act_window">
        <field name="name">Loyalty Cards</field>
        <field name="res_model">loyalty.card</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{'create': False}</field>
        <field name="help" type="html">
            <h1>No Loyalty Cards</h1>
            <p>This sale order does not have any loyalty cards linked to it.</p>
        </field> -->
    </record>

    <record id="view_pos_loyalty_card_tree_inherit" model="ir.ui.view">
        <field name="name">loyalty.card.pos.view.tree.inherit</field>
        <field name="model">loyalty.card</field>
        <field name="inherit_id" ref="loyalty.loyalty_card_view_tree" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='program_id']" position="after">
                <field name="source_pos_order_id"/>
            </xpath>
        </field>
    </record>
</odoo>
